import styled from "@emotion/styled";

const HeaderDiv = styled.section`
  width: 100%;
  height: 200px;
  background-color: wheat;
`;

const Header = () => {
  return <HeaderDiv>여기는 헤더영역입니다.</HeaderDiv>;
};

export default Header;
